---
description: Actix-web 生态评审约束。审查 PR / diff / 设计文档时生效。
globs: "**/*.md"
alwaysApply: false
---

# Actix-web 评审规则

完整规范见 `prompts/reviewer.md`。以下为关键内联规则：

## 评审顺序
1. 模块名与目录结构 → 2. 分层依赖方向 → 3. Handler 职责 → 4. Service 接口定义 → 5. Repository 数据访问 → 6. 错误处理 → 7. 依赖注入 → 8. 配置管理 → 9. 中间件 → 10. 测试 → 11. 文档

## 硬性驳回项（MUST-FIX）
- 模块名使用大写、驼峰命名
- `main` 函数放在非 `src/main.rs` 位置
- Handler 直接操作数据库
- Handler 包含业务逻辑
- Service 引用 `actix_web` 类型（`HttpRequest`、`HttpResponse`、`web::Data` 等）
- 使用 `unwrap()` / `expect()` 处理请求级业务错误
- 使用 `panic!` 处理业务错误
- 忽略错误（`let _ = fallible_fn()`）
- 使用全局 `static` / `lazy_static` 持有依赖
- 硬编码数据库连接串、端口号、密钥
- 未设置连接池参数
- `web::block` 未处理 panic（`Cancelled` 错误）
- 新功能无单测；改功能未同步测试；缺流程级集成测试；僵尸断言
- 测试/编译失败仍提交
- `unsafe` 代码无安全性注释
- 生产 CORS 使用 `Any` 允许所有来源

## 建议性反馈（SHOULD-FIX）
- 函数体超过 50 行
- 函数参数超过 5 个
- 公共 API 缺少文档注释（`///`）
- `clone()` 使用过多
- 未使用 `web::block` 包装同步阻塞操作
- 使用 `as` 做不安全的类型转换

## NIT
- 变量命名不符合 Rust 惯例
- 注释拼写错误
- `use` 语句未合并
- `match` 可用 `if let` 简化

## 已知常见陷阱（识别但不一定驳回）
- `web::block` 闭包中捕获了非 `Send` 类型
- `App::data()` vs `App::app_data()` 混淆（`data` 已废弃）
- `HttpResponse::Ok().json()` vs `web::Json` 响应混淆
- actix Actor 模型与 `async/await` 混合使用时的生命周期问题
- `awc` 客户端在 actix 运行时外无法使用

## 反馈格式
每条反馈含：位置（`file:line`）、级别（MUST-FIX / SHOULD-FIX / NIT / QUESTION）、依据（引用规则条目）、建议（可落地）。

详细规则请读 `prompts/reviewer.md`。
