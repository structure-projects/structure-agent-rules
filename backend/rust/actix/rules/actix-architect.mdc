---
description: Actix-web 生态架构/设计约束。涉及模块划分、分层、API 设计、技术选型时生效。
globs:
  - "**/*.md"
  - "**/Cargo.toml"
alwaysApply: false
---

# Actix-web 架构/设计规则

完整规范见 `prompts/architect.md`（single source of truth）。以下为关键内联规则：

## 硬约束
- Rust 版本 MUST >= 1.75（推荐 1.80+）
- 模块名 MUST 使用 `snake_case`
- 项目 MUST 遵循 Rust 标准项目布局

## 分层架构（Clean Architecture）
- 依赖方向：`handlers → services → repositories → models`（models 无外部依赖）
- **handlers/**：actix-web handler，只做参数提取 + 调用 service + 构建 HttpResponse
- **services/**：业务逻辑，`async fn`，返回 `Result<T, AppError>`
- **repositories/**：trait 定义与实现分离
- **models/**：领域实体、DTO、错误定义，禁止引用框架类型

## 技术选型（推荐）
| 层次 | 推荐 | 替代 | 备注 |
|---|---|---|---|
| Web 框架 | actix-web 4.x | axum | 基于 actix-rt（tokio） |
| 异步运行时 | actix-rt | — | actix-web 内置 |
| ORM / DB | sqlx | sea-orm / diesel | diesel 需 web::block |
| 配置 | config crate | envy | 多环境配置 |
| 日志 | tracing-actix-web | env_logger | 结构化日志 |
| 校验 | validator | garde | 派生宏校验 |
| API 文档 | utoipa | paperclip | OpenAPI 生成 |
| 测试 | actix_web::test | reqwest | 集成测试 |
| HTTP 客户端 | awc | reqwest | actix actors 客户端 |
| 序列化 | serde + serde_json | — | 标准序列化 |
| 错误处理 | thiserror + anyhow | snafu | thiserror 定义错误 |

## 关键约束
- App MUST 通过 `HttpServer::new` + `App::new` 构建
- 共享状态 MUST 通过 `web::Data<T>` + `app_data()` 注入
- 中间件 MUST 通过 `App::wrap()` 注册
- 同步阻塞操作 MUST 用 `web::block` 包装到线程池
- 错误 MUST 使用 `thiserror` + `ResponseError` 统一转换

详细规则、API 设计约束、安全约束请读 `prompts/architect.md`。
