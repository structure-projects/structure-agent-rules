---
description: Koa 生态开发约束。编写 JS 代码时始终生效。
globs:
  - "**/*.js"
  - "**/package.json"
alwaysApply: true
---

# Koa 开发规则

完整规范见 `prompts/developer.md`；组件用法见 `prompts/components.md`；新建项目见 `prompts/project-scaffolding.md`；校验见 `prompts/validation.md`；API 文档见 `prompts/swagger.md`；流水线见 `prompts/ci-cd.md`。以下为关键内联规则：

## 硬约束
- 使用 `async/await`（Koa 基于 async 中间件）
- 错误 MUST 被处理（全局错误中间件 + `ctx.throw`）
- 路由 MUST 使用 koa-router 模块化

## 分层约束
- **Controller**：参数提取 + 调用 Service + 返回响应。禁止写业务逻辑
- **Service**：业务逻辑，禁止引用 `ctx` 对象
- **中间件**：洋葱模型，`await next()` 是分界点

## 关键优先级
- **中间件顺序**：Error Handler → Logger → CORS → Body Parser → Auth → Routes
- **错误处理**：全局中间件 `try { await next() } catch { ... }` → `ctx.throw(status, msg)`
- **校验**：Joi/Zod schema → validate 中间件 → Controller

## 持久化
- Sequelize Model 或 Knex.js Query Builder
- **MUST** 生产环境禁止 `sync()`
- **MUST** 使用版本化迁移（Sequelize CLI 或 Knex migrations）
- **禁止** Controller 直接使用 Model

## Koa Context 关键属性
- `ctx.request.body` — 请求体（需 koa-body）
- `ctx.params` — 路由参数
- `ctx.query` — 查询参数
- `ctx.state` — 中间件间传递数据
- `ctx.throw(status, message)` — 抛出 HTTP 错误

## 测试工作流（MUST）
- 每开发一个功能立即写单测，通过才能做下一个
- 修改功能时同步修改测试
- 业务完成后写集成测试
- 提交前：`npm test` 全部通过
- 禁止测试失败仍提交

详细规则请读 `prompts/developer.md`。
