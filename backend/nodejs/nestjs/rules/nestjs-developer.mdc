---
description: NestJS 生态开发约束。编写 TypeScript 代码时始终生效。
globs: "**/*.ts, **/package.json"
alwaysApply: false
---

# NestJS 开发规则

完整规范见 `prompts/developer.md`；组件用法见 `prompts/components.md`；新建项目见 `prompts/project-scaffolding.md`；校验见 `prompts/validation.md`；API 文档见 `prompts/swagger.md`；流水线见 `prompts/ci-cd.md`。以下为关键内联规则：

## 硬约束
- TypeScript 严格模式（`strict: true`）
- 所有 Provider 类 MUST `@Injectable()`
- 公共方法 MUST 有返回类型注解
- DTO MUST 使用 class-validator 装饰器
- 禁止 `any` 类型（除非有充分理由）

## 分层约束
- **Controller**：只做路由定义 + 参数提取 + 调用 Service + 返回响应
- **Service**：`@Injectable()` + 构造器注入 + NestJS 内置异常类
- **Repository**：TypeORM `@InjectRepository` 或 Prisma Service
- **Module**：正确声明 imports/controllers/providers/exports

## 关键优先级
- **DI**：构造器注入（NestJS IOC）→ 禁止手动 new
- **异常**：NestJS 内置异常类 → 全局 ExceptionFilter → 统一响应格式
- **校验**：class-validator DTO → ValidationPipe 全局注册 → whitelist + transform

## 命名约定
- 文件名：`kebab-case`（`user.service.ts`）
- 类名：`PascalCase`（`UserService`、`UserController`）
- DTO：`{Action}{Module}.dto.ts`（`create-user.dto.ts`）
- Entity：`{Module}.entity.ts`（`user.entity.ts`）

## 持久化
- **MUST** TypeORM `synchronize: false`（生产环境）
- **MUST** 使用版本化迁移（TypeORM Migration 或 Prisma Migrate）
- **MUST** 通过 `@InjectRepository` 或 PrismaService 访问数据库
- **禁止** Controller 直接注入 `DataSource`/`EntityManager`

## 全局配置
- **MUST** 全局注册 `ValidationPipe`（`whitelist: true, transform: true`）
- **MUST** 启用 `helmet()` 安全头
- **MUST** 启用 CORS 白名单
- **SHOULD** 启用 `enableShutdownHooks()`

## 测试工作流（MUST）
- 每开发一个功能立即写单测，通过才能做下一个
- 修改功能时同步修改测试
- 业务完成后写 E2E 测试
- 提交前：`npm test` + `npm run test:e2e` 通过 + `npm run build` 编译通过
- 禁止测试/编译失败仍提交

详细规则请读 `prompts/developer.md`。
