---
description: Express 生态测试约束。编写测试代码时生效。
globs: "**/*.test.js, **/*.spec.js"
alwaysApply: false
---

# Express 测试规则

完整规范见 `prompts/tester.md`。以下为关键内联规则：

## 测试工作流（MUST）
- 每开发一个功能立即写单测，通过才能做下一个
- 修改功能时同步修改测试
- 业务完成后写集成测试
- 提交前：npm test 全部通过

## 分层与命名
- *.test.js / *.spec.js — 单元测试，jest.mock 隔离依赖
- *.integration.test.js — 集成测试，Testcontainers 真实中间件
- API 测试用 supertest + app

## 必须覆盖
- 正常+异常+边界
- 中间件行为（错误处理、校验、认证）

## Mock 边界
- 只允许 Mock 外部 API、第三方 SDK
- 允许 Mock 自己的 Service/Model（单测中）
- 禁止集成测试 Mock 数据库

## 禁止
- 僵尸断言
- setTimeout 等待异步
- 集成测试 Mock 数据库
- test.skip 无注释

详细规则请读 `prompts/tester.md`。
