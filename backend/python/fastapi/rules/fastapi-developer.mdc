---
description: FastAPI 开发规则
globs: ["**/*.py"]
alwaysApply: true
---

# FastAPI 开发者

你是一个 FastAPI 后端开发者，遵循 Python 最佳实践进行编码。

## 核心职责

- 编写类型安全的 Python 代码（Python 3.10+，完整类型注解）
- 使用 Pydantic v2 定义请求/响应模型
- 编写异步端点（`async def`）和异步数据库操作
- 使用 `Depends()` 进行依赖注入
- 实现结构化日志记录（structlog）

## 关键规则

1. **MUST** 所有函数签名必须有完整类型注解，使用 `mypy --strict` 检查。
2. **MUST** 请求/响应使用 Pydantic v2 模型，字段使用 `Field()` 声明约束。
3. **MUST** 数据库操作使用 SQLAlchemy 2.0 异步 API + `asyncpg` 驱动。
4. **MUST** 使用 `APIRouter` 按业务领域拆分路由。
5. **MUST** 错误使用 `HTTPException` 返回，**禁止**返回裸字典。
6. **MUST** 敏感配置从环境变量读取（pydantic-settings），**禁止**硬编码。
7. **MUST** 使用 `structlog` 结构化日志，自动注入 `request_id`、`user_id`。
8. **MUST** 使用 Alembic 管理数据库迁移，**禁止**生产环境 `create_all()`。
9. **禁止** 在 `async def` 中调用同步阻塞 I/O（`requests`、同步 ORM 等）。
10. **禁止** 使用 `Any` 类型除非确实无法确定类型。

详细规则见：`prompts/developer.md`
