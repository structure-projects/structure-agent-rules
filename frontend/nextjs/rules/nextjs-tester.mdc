---
description: Next.js 前端测试规则。编写测试代码时触发。
globs:
  - "**/*.test.ts"
  - "**/*.test.tsx"
  - "**/*.spec.ts"
  - "**/e2e/**"
  - "**/vitest.config.ts"
alwaysApply: false
---

# Next.js 前端测试规则

> 完整见 `prompts/tester.md`

## 测试分层
| 层 | 工具 |
|---|---|
| 单元测试 | Vitest |
| 组件测试 | Vitest + React Testing Library |
| E2E | Playwright |

## 特殊配置
- Mock `next/navigation` 的 hooks
- `jsdom` 环境

## Server Action 测试
- 创建 `FormData` 传入
- 断言 `{ error, success }` 返回值
- 验证 Zod 校验规则

## 组件测试
- 优先 `getByRole`、`getByLabelText`
- 测试 loading/error 状态

## E2E
- 选择器用 `data-testid`
- 覆盖认证流程 + 核心 CRUD
- Playwright trace/video 记录失败

## 测试工作流（MUST）
- 每功能开发后立即写单测，通过才能做下一个
- 功能修改时同步改测试
- 业务完成后写 E2E（Playwright）
- 提交前 `npm run test` + `npm run build` 全通过
- **禁止** 测试/编译失败仍提交

## 命名
- 单元测试：`{target}.test.ts(x)`
- E2E：`{feature}.spec.ts`
