---
description: Next.js 前端开发约束。编写 Next.js/React/TS 代码时始终生效。
globs:
  - "**/*.tsx"
  - "**/*.ts"
  - "**/app/**"
  - "**/middleware.ts"
  - "**/package.json"
  - "**/next.config.*"
alwaysApply: true
---

# Next.js 前端开发规则

完整规范见 `prompts/developer.md`；组件用法见 `prompts/components.md`；新建项目见 `prompts/project-scaffolding.md`；流水线见 `prompts/ci-cd.md`。以下为关键内联规则：

## 硬约束
- **MUST** Next.js 14+ App Router（`app/` 目录）
- **MUST** TypeScript strict mode + Tailwind CSS
- **MUST** React Server Components 为默认，按需 `'use client'`

## Server vs Client Components
- **MUST** 默认使用 Server Component
- **MUST** 仅在需要交互时添加 `'use client'`
- **禁止** Server Component 中使用 `useState`/`useEffect`

## Server Actions
- **MUST** `'use server'` 声明
- **MUST** Zod 校验输入
- **MUST** 操作后 `revalidatePath`/`revalidateTag`

## 路由
- **MUST** App Router（非 Pages Router）
- **MUST** `next/navigation`（非 `next/router`）
- **MUST** 路由组 `(groupName)` + `loading.tsx` + `error.tsx`

## 数据获取
- Server Component 中直接 async/await
- 并行请求用 `Promise.all`

## 认证
- next-auth (Auth.js) + Middleware
- `config.matcher` 精确限定

## 数据库
- Prisma Client 单例模式
- 用 `prisma migrate dev/deploy`

## 优化
- **MUST** `next/image` 处理图片
- **MUST** `next/font` 处理字体

## 环境变量
- 服务端密钥不加前缀
- 客户端用 `NEXT_PUBLIC_` 前缀

## 测试工作流（MUST）
- 每开发功能立即写单测，通过才能做下一个
- 功能修改时同步改测试
- 业务完成后写 E2E（Playwright）
- 提交前 `npm run test` + `npm run lint` + `npm run build` 全通过
- **禁止** 测试/编译失败仍提交

详细规则请读 `prompts/developer.md`。
